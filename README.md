# OPR Card Generator

Small Parcel/TypeScript app that generates printable TCG-sized cards from a shared Army Forge list.

## Scripts

```sh
yarn start
yarn build
yarn typecheck
```

On Windows PowerShell, use `yarn.cmd` if script execution policy blocks `yarn.ps1`.
